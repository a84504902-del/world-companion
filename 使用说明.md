## AI Companion 使用说明

### 启动方式

双击 `启动.bat`，或在项目目录下运行：

```
venv\Scripts\python.exe app.py
```

启动后浏览器访问 `http://localhost:8877`（端口可在 `.env` 中修改 `PORT` 值）。后台管理面板地址为 `http://localhost:8877/admin`。

### 依赖安装

首次使用或换机器时，运行：

```
venv\Scripts\pip.exe install -r requirements.txt
```

所有依赖已锁定到精确版本（aiohttp 3.14.1、edge-tts 7.2.8、sentence-transformers 5.6.0、torch 2.12.1），不会出现因依赖升级导致的兼容性问题。

### 配置说明

项目根目录的 `.env` 文件用于配置各项参数：

- `HOST` / `PORT` — 服务监听地址和端口，默认 `0.0.0.0:8877`
- `TTS_VOICE` — TTS 语音角色，默认 `zh-CN-XiaoyiNeural`
- 各 LLM 的 API Key（`DEEPSEEK_API_KEY`、`ZHIPU_API_KEY` 等）— 在后台管理页面或 `.env` 中配置

### 核心功能

**多会话聊天** — 左侧栏可新建、切换、重命名、删除会话，每个会话的对话历史完全独立，多标签页同时打开也不会互相干扰。

**向量记忆** — AI 会自动从对话中提取关键事实并存储为向量记忆，后续对话时通过语义检索召回相关记忆，让 AI "记住"你说过的话。也可以在聊天中输入 `保存记忆：xxx` 手动保存。后台管理面板的记忆页签可以查看和删除记忆。

**人物关系** — 在后台管理面板中添加人物和关系（如"小明是我的朋友"），AI 在对话时会参考这些关系信息。

**多 LLM 切换** — 支持 DeepSeek、智谱 GLM、通义千问、百度文心、Ollama 本地模型等多家后端，可在聊天界面的模式下拉框中切换。也支持通过后台管理页面添加自定义 LLM。

**语音功能** — 点击聊天界面的语音按钮可启用 TTS 语音播报，浏览器端的麦克风按钮支持语音输入转文字。

**提示词模板** — 后台管理面板提供 26 个预置提示词模板（女友、傲娇、翻译官、程序员等），可为不同会话设置不同的 AI 人设。

**聊天导出** — 支持将聊天记录导出为 JSON、Markdown、TXT 三种格式。

### 日志

启动后控制台会输出带时间戳的日志，格式为 `时:分:秒 [模块名] 级别: 内容`，例如：

```
21:07:59 [embedding] INFO: 加载嵌入模型 all-MiniLM-L6-v2 ...
21:08:02 [embedding] INFO: 模型加载完成
21:08:02 [retriever] INFO: 向量缓存预加载完成: 15 条
```

如果遇到问题，查看日志中的 `ERROR` 级别信息可以快速定位原因。

### 文件结构

```
app.py                  主入口，启动 Web 服务、数据库备份
config.py               读取 .env 配置
db.py                   SQLite 数据库操作（共享连接）
llm.py                  统一 LLM 调用接口（含流式输出）
embedding.py            本地向量嵌入（sentence-transformers）
memory_retriever.py     语义记忆检索 + 自动事实提取
tts.py                  Edge-TTS 语音合成
log.py                  日志配置
routes/                 API 路由
  chat.py               聊天（流式输出）、会话管理
  memory.py             记忆增删查、权重管理
  person.py             人物管理
  relation.py           关系管理
  custom_llm.py         LLM 配置管理（含流式调用）
  template.py           提示词模板管理
  admin.py              后台统计与数据库管理
static/js/              前端模块
  app.js                主入口，模块注册
  state.js              共享应用状态
  chat.js               聊天逻辑（流式渲染）
  sessions.js           会话管理
  memory.js             记忆/人物/关系/模板/搜索
  tts.js                语音输入/输出
  utils.js              工具函数
  ui.js                 面板切换
static/css/             样式文件
templates/              HTML 页面模板
backups/                数据库自动备份（最近 7 份）
memory.db               SQLite 数据库文件
```

### 更新记录

**2026-07-08（第二轮优化）**

**LLM 流式输出** — 聊天回复现在逐字流式显示，不再等待全部生成完毕。支持 OpenAI 兼容接口和 Ollama 的流式输出，百度文心因接口限制自动回退到非流式模式。前端使用 `ReadableStream` 消费 SSE 事件，配合闪烁光标动画，体验接近 ChatGPT。

**数据库连接复用** — `db.py` 从"每次操作开关连接"改为共享长连接模式。启动时创建一个 SQLite 连接（WAL 模式），所有操作复用，关闭应用时统一释放。减少了频繁开关连接的开销，对话响应更快。

**记忆管理增强** — 新增记忆权重功能（`weight` 字段，默认 1.0），高权重记忆在检索时优先召回。后台记忆面板按权重降序排列，支持标签显示。新增"从消息保存记忆"API，可在聊天界面将任意消息标记为记忆。

**前端模块化** — 原 `app.js`（760 行）拆分为 7 个 ES Module：`state.js`（状态）、`chat.js`（聊天）、`sessions.js`（会话）、`memory.js`（记忆/人物/关系）、`tts.js`（语音）、`utils.js`（工具）、`ui.js`（面板）。各模块职责清晰，后续维护更方便。

**自动备份** — 每次启动应用时自动备份 `memory.db` 到 `backups/` 目录，文件名带时间戳。自动保留最近 7 份备份，旧的自动清理。后台管理面板也提供手动备份按钮（`POST /api/database/backup`）。

**2026-07-08（第一轮优化）**

**会话状态隔离** — 每个会话拥有独立的 `SessionState` 对象，对话历史、消息计数、自动摘要触发等全部按会话隔离，多标签页同时操作不再互相干扰。

**日志系统** — 全局 `print()` 替换为 Python `logging` 模块，新增 `log.py` 配置入口。日志带时间戳和模块标识，便于排查问题。

**记忆向量嵌入修复** — 手动保存的记忆现在也会生成向量嵌入，确保语义搜索能召回所有记忆。

**依赖版本锁定** — `requirements.txt` 锁定到精确版本，新增 `torch` 依赖锁定。
